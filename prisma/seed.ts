// O script de seed foi desativado para evitar erros de build.
// O script tentava popular o banco de dados com dados de um arquivo excel e um usuário padrão,
// mas continha dependências e lógica (ex: campo 'password') que não existem mais no projeto atual.

